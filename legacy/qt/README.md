# Archived Qt implementation

This directory is excluded from current builds and release workflows. It keeps
the former Qt source, packaging scripts, CI files, and visual resources solely
for feature-parity research and historical recovery.

Current application assets live only in `assets/`. Nothing in `archive/`,
`scripts/`, `ci/`, or `site/` is permitted as an input to a current package.
