# Changelog

## Unreleased

- Reorganized maintained applications under `platforms/macos` and
  `platforms/desktop`.
- Centralized current branding and package icons under `assets`.
- Standardized generated packages under `artifacts/releases` by operating
  system.
- Isolated the former Qt implementation and its obsolete workflows under
  `legacy/qt`.
- Added shared rclone update parsing and consistent macOS/Tauri update sheets.
- Added dual-pane Windows/Linux interface parity work and cross-platform package
  verification jobs.

The historical Qt changelog is retained at `legacy/qt/CHANGELOG.md`.
